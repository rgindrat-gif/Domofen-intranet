{
  "name": "domofen-intranet",
  "version": "1.0.0",
  "description": "Frontend JS pour l'intranet B2B Domofen (espace partenaires)",
  "scripts": {
    "build": "npx terser src/domofen-forms.js -o domofen-forms.min.js --compress --mangle",
    "watch": "npx nodemon --watch src -e js --exec 'npm run build'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rgindrat-gif/Domofen-intranet.git"
  },
  "author": "Nuavo Holdings AG",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "terser": "^5.37.0"
  }
}
